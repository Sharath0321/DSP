FUNCTION FILE: (save the file name as linearConv.m)
function output = linearConv (x, y)
% Pad the sequences with zeros to have equal length
N=length(x) +length(y)-1;
x_pad =[x, zeros(1, N - length(x))];
y_pad =[y, zeros(1, N - length(y))];

% Compute the DFT of the padded sequences
X = fft(x_pad);% Try with direct computation too
Y = fft(y_pad);

% Perform point-wise multiplication in the frequency domain
Z = X.* Y;
% Compute the inverse DFT of the product
z = ifft(Z);
% Take the real part of the result (due to numerical precision)
output = real(z);
end

MAIN FILE:
clc;
close all;
clear all;

%ASSIGN INPUTS
x = [1, 3, 5, 3];
h= [3, 5, 4, 0];

% COMPUTE LINEAR CONVOLUTION 
y= linearConv(x, h);

% DISPLAY THE RESULT
disp('y=')
disp(y)
￼Enter
